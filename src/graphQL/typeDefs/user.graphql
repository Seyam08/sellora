type Greetings {
  first: String
  last: Boolean
}
type Test {
  text: String!
  state: Boolean
}
type User {
  id: ID!
  name: String!
}
type Query {
  greetings: Greetings
  hello: Test!
}
type Mutation {
  createUser(name: String!): User
  updateUser(name: String): User
}
